# MLProject
name: workflow-ci

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_path: {type: str, default: "namadataset_preprocessing/data_clean.csv"}
    command: "python modelling.py --data_path {data_path}"
